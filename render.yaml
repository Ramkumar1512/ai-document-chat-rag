services:
  - type: web
    name: rag-backend
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn rag_backend.rag_backend.wsgi:application --bind 0.0.0.0:$PORT"